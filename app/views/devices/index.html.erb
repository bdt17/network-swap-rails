<div class="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100 py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-7xl mx-auto">
    <div class="text-center mb-12">
      <h1 class="text-5xl font-bold text-gray-900 mb-4">🚀 Thomas IT Network Swap</h1>
      <p class="text-xl text-gray-600">Rails 8.1 + PostgreSQL - Phoenix DC Inventory</p>
    </div>
    
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      <% @devices.each do |device| %>
        <div class="bg-white rounded-xl shadow-lg p-6 hover:shadow-xl transition-all duration-300">
          <div class="flex items-center justify-between mb-4">
            <h3 class="text-2xl font-bold text-gray-900"><%= device.mac %></h3>
            <span class="px-3 py-1 rounded-full text-sm font-semibold 
              <%= device.status == 'online' ? 'bg-green-100 text-green-800' : 'bg-yellow-100 text-yellow-800' %>">
              <%= device.status %>
            </span>
          </div>
          <div class="space-y-2 text-gray-600">
            <p><strong>IP:</strong> <%= device.ip %></p>
            <p><strong>Location:</strong> <%= device.location %></p>
          </div>
          <div class="mt-6 pt-4 border-t">
            <a href="/api/devices" class="text-blue-600 hover:text-blue-800 font-semibold">View JSON API →</a>
          </div>
        </div>
      <% end %>
    </div>
    
    <div class="mt-12 text-center">
      <a href="/api/devices" class="bg-blue-600 text-white px-8 py-3 rounded-lg text-lg font-semibold hover:bg-blue-700 transition-colors">
        🔌 API Endpoints
      </a>
    </div>
  </div>
</div>
